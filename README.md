# UnitConverter - iOS App for unit conversion

## 🧠 Features

- ✅ Easy-to-use interface with form-based input
- ⏱️ Supports conversion between:
  - Seconds
  - Minutes
  - Hours
  - Days
- 📱 Optimized for iPhone with number pad input and focus management

## 🛠️ Built With

- `@State` and `@FocusState` for reactive UI updates
- Swift `Picker` and `TextField` components for user input
